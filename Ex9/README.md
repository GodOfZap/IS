# Brute-Force Guessing Script (`password_guessing.py`)

## Overview

Generates all lowercase letter combinations to guess a user-provided password.

## How It Works

- Enter the password.
- The script tries guesses systematically until it finds the match.

## Usage

Run the script and input your password when prompted.
